import { AseTypographyDirective } from './ase-typography.directive';

describe('AseTypographyDirective', () => {
  it('should create an instance', () => {
    const directive = new AseTypographyDirective();
    expect(directive).toBeTruthy();
  });
});
